# Benchmark Scripts

This folder is reserved for model-comparison benchmark scripts.

Current script:

- `compare_models.py`: compare two checkpoints (legacy or NAS) under a unified channel-aware protocol, and export JSON/CSV/Markdown/figures.

Use `python scripts/benchmark/compare_models.py --help` for full options.
